GCP_PROJECT_ID=navisound
GCP_REGION=us-central1
GEMINI_MODEL=gemini-2.0-flash-live-preview-04-09
POSTGRES_URL=postgresql://postgres:localdev@localhost:5432/navisound
REDIS_URL=redis://localhost:6379
EXPRESS_PORT=3000
FASTAPI_PORT=8000
NODE_ENV=development

# Gemini native audio generation for the AudioFeedback agent.
# Options: TEXT (default — uses browser SpeechSynthesis), AUDIO (Gemini-generated PCM),
#          TEXT,AUDIO (both — text for structured params, audio for spatial cues)
NAVISOUND_AUDIO_MODALITY=TEXT
